{
  "dependencies": {
    "@emailjs/browser": "^3.11.0",
    "@syncfusion/ej2-react-charts": "^21.2.5",
    "react-select": "^5.7.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "server": "cd.. && cd Backend && npm run dev",
    "instalar": "cd Front && npm install && cd Backend && npm install",
    "dev": "concurrently --kill-others-on-fail \"npm start\" \"npm run server\"",
    "cpt": "cd Front && npm run dev" 
  }
}
